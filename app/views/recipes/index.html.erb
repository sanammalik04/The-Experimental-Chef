  
<% @all_types.each do |hash| %>
    <h2><%= hash[:type_name] %> Recipes</h2>

    <% hash[:type].each do |recipe| %>
        <ul>
                <%= image_tag(recipe.image_url, size: "200x190")%>
            <li><%= link_to recipe.name, recipe_path(recipe) %></li>
        </ul>
    <% end %>
<% end %>




